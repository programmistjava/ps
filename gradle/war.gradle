apply plugin: "war"

bootWar {
    mainClassName = "com.peopleflow.app.PeopleFlowApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
